const config = {
    basename: '',
    defaultPath: '/login',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12,
    SERVER_IP: 'https://voice-bot-backend.herokuapp.com/api'
};

export default config;
